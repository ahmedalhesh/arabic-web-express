name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy to Cloudflare Pages
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build
        env:
          NODE_ENV: production
          SESSION_SECRET: ${{ secrets.SESSION_SECRET || 'ArabicWebExpress2024SecretKey' }}
          ENCRYPTION_KEY: ${{ secrets.ENCRYPTION_KEY || 'ArabicWebExpress2024SecretKey32' }}

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: arabic-web-express
          directory: dist
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
          branch: main
          wranglerVersion: '3.0.0'
        env:
          SESSION_SECRET: ${{ secrets.SESSION_SECRET || 'ArabicWebExpress2024SecretKey' }}
          ENCRYPTION_KEY: ${{ secrets.ENCRYPTION_KEY || 'ArabicWebExpress2024SecretKey32' }}

